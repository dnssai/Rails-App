<!DOCTYPE html>
<html>
<head>
  <title>Articles</title>
  <style>
    h1 {
      color: #007bff;
      text-align: center;
    }

    ul {
      list-style-type: none;
      padding: 0;
    }

    li {
      border-bottom: 1px solid #ddd;
      padding: 10px;
      background-size: 20px 20px;
      background-repeat: no-repeat;
      background-position: left center;
    }

    .offset-text {
      padding-left: 30px;
    }

    .container {
      margin-top: 10px;
    }

    <% @posts.each do |post| %>
      <% image_name = post.name.parameterize.downcase %>
      <% image_path = Rails.root.join('app/assets/images/villes', "#{image_name}.png") %>
      <% if File.exist?(image_path) %>
        .post-<%= image_name %> {
          background-image: url('<%= asset_path("villes/#{image_name}.png") %>');
        }
      <% end %>
    <% end %>
  </style>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
</head>
<body>
  <h1>Articles</h1>

  <ul>
    <% @posts.each do |post| %>
      <% image_name = post.name.parameterize.downcase %>
      <li class="post-<%= image_name %>">
        <strong class="offset-text"><%= post.name %></strong>: 
        <div class="container">
          <a href="<%= post_path(post) %>" class="btn btn-primary">Lire la suite</a>
          <a href="<%= edit_post_path(post) %>" class="btn btn-secondary">Éditer</a>
          
          <%= form_with(model: post, method: :delete, local: true) do |form| %>
            <%= form.submit 'Supprimer', data: { confirm: 'Êtes-vous sûr de vouloir supprimer cet article ?' }, class: 'btn btn-danger' %>
          <% end %>
        </div>
      </li>
    <% end %>
    <a href="<%= new_post_path %>" class="btn btn-success">Créer un article</a>
  </ul>

  <% if notice %>
    <div class="alert alert-success">
      <%= notice %>
    </div>
  <% end %>

  <%= javascript_importmap_tags %>
</body>
</html>
